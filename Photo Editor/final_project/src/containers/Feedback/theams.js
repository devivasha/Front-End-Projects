import { createMuiTheme } from '@material-ui/core/styles'
const themes = createMuiTheme({
    palette: {
        error: {
            main: '#EA4165',
        },
        primary: {
            main: '#212052',
        }
    },
})

export default themes